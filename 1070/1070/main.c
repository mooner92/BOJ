#include <stdio.h>
#include <string.h>

int main(){
    int a,b,c;
    scanf("%d %d",&a, &b);
    int ans=0;
    char s[2500];
    char g[2500];
    char t[2500];
    
    scanf("%s",&s);
    scanf("%s",&g);
    int i=0;
    
    
    /*while(i<(a*b)-1){
        
        if(&s[i] == "*"){
            ans == i;
        }
        else i++;
        int res = strcmp(&s[i], "-");
        if(res==0){
            i++;
        }
        else{
            t[i] = s[i];
            i++;
        }
        
    }*/
}
